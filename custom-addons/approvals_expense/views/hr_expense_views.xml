<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
    
        <record id="hr_expense_sheet_view_form_inherit_approval" model="ir.ui.view">
            <field name="name">hr.expense.sheet.view.form.inherit</field>
            <field name="model">hr.expense.sheet</field>
            <field name="inherit_id" ref="hr_expense.view_hr_expense_sheet_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button string="Request" name="generate_approval_request" type="object" states='draft,need_improvement' class="oe_highlight"/>
                </xpath>
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_view_approval_request" type="object" class="oe_stat_button" icon="fa-list">
                        <field name="approval_count" string="Approval" widget="statinfo"/>
                    </button>
                </xpath>
                <xpath expr="//button[@name='action_submit_sheet']" position="attributes">
                    <attribute name="invisible" value="1">1</attribute>
                </xpath>
                <field name="payment_mode" position="after">
                    <field name="date" />
                </field>
                <field name="accounting_date" position="after">
                    <field name="team_id" />
                </field>
                <xpath expr="//button[@name='reset_expense_sheets']" position="attributes">
                    <attribute name="attrs" value="{'invisible': ['|', ('can_reset', '=', False), ('state', 'not in', ['submit', 'cancel'])]}">{'invisible': ['|', ('can_reset', '=', False), ('state', 'not in', ['submit', 'cancel'])]}</attribute>
                </xpath>
            </field>
        </record>

        <record id="hr_expense.action_hr_expense_sheet_all" model="ir.actions.act_window">
            <field name="context">{ 'searchpanel_default_state': ["draft", "submit", "approve", "post", "done", "request", "need_improvement"] }</field>
        </record>

        <record id="hr_expense_view_form_inherit_qty" model="ir.ui.view">
            <field name="name">hr.expense.view.form.inherit</field>
            <field name="model">hr.expense</field>
            <field name="inherit_id" ref="hr_expense.hr_expense_view_form"/>
            <field name="arch" type="xml">
                <field name="employee_id" position="before">
                    <field name="qty" />
                    <field name="uom_id" />
                </field>
            </field>
        </record>
    
    </data>

</odoo>
